# Docker Compose instructions for Windows:

```bash
docker compose --profile windows -f docker-compose.yml -f docker-compose.windows.yml up
```

# Docker Compose instructions for Linux

```bash
docker compose --profile linux up
```
