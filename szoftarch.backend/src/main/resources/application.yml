spring:
  application:
    name: szoftarch.backend
  datasource:
    url: jdbc:mysql://localhost:3306/plant_care
    username: root
    password: david
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 5000
